/* eslint-disable no-template-curly-in-string */
// TODO: translate more fields from https://github.com/jquense/yup/blob/master/src/locale.js

const mixed = {
  required: 'Pole jest wymagane',
};

const string = {
  min: 'Minimum ${min} znaków',
  max: 'Maximum ${max} znaków',
  email: 'Wpisz poprawny adres e-mail',
};

const number = {};

const date = {};

const boolean = {};

const object = {};

const array = {};

export default {
  mixed,
  string,
  number,
  date,
  object,
  array,
  boolean,
};
